/// The passenger dashboard reuses the exact same design system as the
/// landing page (colors, typography, glass/glow helpers) so both feel like
/// one product. Importing this single file gives every passenger widget
/// access to `LandingColors`, `LandingType` and `LandingEffects`.
export '../landing/theme/colors.dart';
export '../landing/theme/effects.dart';
export '../landing/theme/typography.dart';
